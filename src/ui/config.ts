export const enum Colors {
  BUTTON_BACK = 0x000000,
  HIGHLIGHT = 0xFF0000
}

export const enum Alphas {
  // button states
  BUTTON_NORMAL = 0.1,
  BUTTON_OVER = 0.15,
  BUTTON_DOWN = 0.3,
  // preview parts
  PREVIEW_ALPHA = 0.5
}

export const enum Delays {
  FLIP = 0.25
}